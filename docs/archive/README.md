# Archive

这里存放已归档的 PoC、阶段性、概念性与历史文档。

## 说明

- 档案文件默认不再作为当前实现依据
- 它们保留历史决策、概念探索和迁移参考价值
- 如果其中内容与 `docs/v2/` 或 `packages/contracts/` 冲突，以后者为准

## 子目录

- `legacy-docs/`
  原 `docs/` 下的历史方案、PRD、方法学与附录
- `root-docs/`
  原仓库根目录中的阶段报告、PoC 指南与执行说明
